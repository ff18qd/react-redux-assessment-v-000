{
  "name": "Nextstop",
  "engines": {
    "node": "6.3.1"
  },
  "scripts": {
    "build": "cd nextstop-client && npm install && npm run build && cd ..",
    "deploy": "cp -a nextstop-client/build/. public/",
    "postinstall": "npm run build && npm run deploy && echo 'Client built!'"
  }
}